import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'Site Name',
  mode: 'site',
   publicPath: '/myselfThing/',
  base: '/ myselfThing',
  // more config: https://d.umijs.org/config
});
